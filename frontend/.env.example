# Local dev: leave empty (Vite proxy forwards /api to localhost:5000)
# VITE_API_URL=

# Split deploy (frontend on Vercel/Netlify, backend elsewhere):
# VITE_API_URL=https://your-backend.onrender.com/api
